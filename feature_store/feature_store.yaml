project: inferstream

provider:
  registry:
    type: local
    path: /tmp/feast/registry

online_store:
  type: redis
  connection_string: "redis://localhost:6379"

offline_store:
  type: duckdb
  database: "/data/inferstream.duckdb"

entity_key_serialization_version: 2
