apiVersion: 1

providers:
  - name: InferStream
    orgId: 1
    folder: InferStream
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
